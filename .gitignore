# children's work, renders, print packs — never in git
data/
*.jpg
*.jpeg
*.png
*.pdf
!docs/sources/*.pdf

# secrets
.env
.env.*
!.env.example
supabase/.temp/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.coverage
htmlcov/

# node
node_modules/
.next/
apps/web/.env*.local

# n8n runtime
n8n/.n8n/

# os / editor
.DS_Store
.idea/
.vscode/
.tokensave

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# web app
apps/web/node_modules/
apps/web/.next/
apps/web/.next-test/
apps/web/test-results/
apps/web/.design-approved.json
.design-approved.json
.vercel
.env*

# aislop session logs — the scanner's own working notes, regenerable
**/.aislop/*.jsonl

# rendered by research/spine_page.py from docs/spine/spine.json; regenerate, do not commit
docs/spine/index.html
