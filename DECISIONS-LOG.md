# Decisions log

Newest first. One line per decision: date, decision, why, source. Rejected alternatives get an ADR.

| Date | Decision | Why | Source |
|---|---|---|---|
| 2026-09-20 | **A goal is a file, not a paragraph: `goals/<name>.yaml` + `engine goal <name>` (ADR 0015).** One sentence saying what the thing must do, `scenarios` that prove it on real requests checked independently of the code that answered them, and `criteria` for the whole-repo checks. The bar is 100% — 19 of 20 fails — and the command exits non-zero until it passes. Beside it `engine audit`: twelve named invariants over every row, so a new class of bug becomes an invariant instead of a test that trips over it. `bin/engine` makes both runnable from any directory. W1's goal: 10/10 scenarios, 5/5 criteria, achieved. Rejected: prose gates in BUILD-ORDER (what we had — three sessions drifted), the unit suite as the definition of done (294 tests never noticed that multiplication questions had no diagnosable wrong answer), a hygiene-only checklist, and model-calling scenarios (cost and wobble; the model has its own eval). | Nimish: "with every test you are finding an error — how do we know that we don't have any more errors", and "the system should keep on working towards it till 100% accuracy is achieved". Running the two commands immediately found four defects nothing was looking at: multiplication undiagnosable (14 of 20 questions), the `op` constraint with no `×`, 1,172 live items naming six mistakes absent from the vocabulary, and the app querying `op = 'both'` which the data never uses. | nimish (chat, 2026-09-20); ADR 0015; `CLAUDE.md` rules 12-13; `goals/w1-build-the-bank.yaml`; `STATE.md` |
| 2026-09-19 | **The list of mistakes a learning objective can produce is an engine output, not a teacher's checklist, and it has two halves (ADR 0014 + its amendment).** Code enumerates every named wrong method the numbers a band's rule allows can actually produce, by building items with the generators the bank itself uses (`assess/bands.py`); the prompt `misconception_list` (v4) is told what that covered and asked only for what code cannot reach — misread stories, methods with no predictor, reasoning slips. Every proposal is checked: wrong arithmetic is dropped, a wrong answer an existing predictor reproduces *is* that misconception, and an "identifiable from the answer" claim code cannot reproduce is downgraded to the working. `engine bank misconceptions <set> [--apply]`; applying unions and withdraws the set's ratification. Recall against curated lists moved from eval to test, and the prompt's eval became what survives as an addition: 0.75, ₹0.53 a skill set. Rejected: scoring a model on rediscovering what arithmetic can enumerate (v1 0.40, v2 0.53 — the wrong question), teachers ticking misconceptions, matching proposals by name, showing the model the vocabulary blind-free, and a bigger model (sonnet 4/9 vs haiku 3/9 at eight times the cost). | Nimish: "A certain learning objective having all possible conceptions is the job of the engine, not of the teacher", then: "do not leave for anything later; whenever you find an issue — don't apply a band aid; solve the root cause." The guard found four specs claiming mistakes the engine could not mark: two lists corrected, two predictors written. | nimish (chat, 2026-09-19); ADR 0014; `CLAUDE.md` rule 11; `STATE.md` |
| 2026-09-19 | **Nimish ratifies all 17 skill-set specs himself, by command, with his name on every row (`engine ratify --by "Nimish Shah"`; ADR 0013) — closing W1 gate 1 and with it all six W1 gates. W2 opens.** Neha, Achal and Aseem correct the specs afterwards in the screen; a content edit withdraws the ratification automatically, so no signature ever outlives the words it approved. Rejected: waiting for Aseem (blocks W2 on other people's calendars for specs that are the engine's own drafts of the school's own documents), seventeen clicks in the app (not a command a gate can quote), and a `system`/`founder` literal in `ratified_by` (destroys the one thing the column is for). | Nimish, having reviewed the sets: "let's programmatically ratify all the 16 combinations I have reviewed, and we can start building ahead." The three questions still open under gate 1 are pedagogy — the reviewer's objection to mixing formats inside an exchange band, one gold-set disagreement, the G1 floors — and none of them is answered faster by the repository waiting. | nimish (chat, 2026-09-19); ADR 0013; `BUILD-ORDER.md` W1 gate 1; `STATE.md` |
| 2026-09-19 | **W1 gate 4 amended before building it, after Nimish read an external architecture proposal: two narrow advisory reviewers instead of one blended validator; difficulty checked as measured dimensions against `item.tags`, not asserted by a label; and versioned rules with provenance on every item.** Also adopted: the misconception analyst (`engine bank unclassified`) as part of W1's finish line. Parked to W2/W4 with a line in `BUILD-ORDER.md`: one Assessment Specification contract, and the policy engine framed as information gain. Kept deliberately against the proposal: template-level trust rather than per-item approval (a bank of thousands cannot have a reader), and one workflow at a time. | The proposal was written from the same v0.1 spec this repo implements, so most of it was already built — but three of its points were cheap now and expensive once children are answering. The dimensional check earned itself immediately: it found 93 items sitting in bands whose rule they no longer satisfied, invisible until something measured them. | nimish (chat, 2026-09-19, "go ahead and do the needful"); `assessment_engine_architecture(1).pdf` §7, §8.2/8.3, §15; ADR 0012; `BUILD-ORDER.md` W1 gate 4 |
| 2026-09-19 | **W1 gate 2's target becomes "50 items, or the unit's whole enumerable universe, whichever is smaller" (ADR 0011).** A unit under 50 carries its measured floor as `min_items` in its own skill-set row, and only after a fresh fill accepts zero new items — the evidence that the number is a ceiling, not a stopping point. Gate 2 then closed: 64 of 64 units at or above target, 3,373 live items, ₹0 of model spend. Rejected: widening R1/R2's number ranges (redefines what the rung teaches — Aseem's call, and it blurs R1 into R2), and making a word problem's story part of item identity (closes the gap honestly but rewrites the key of every stored word problem; revisit with a migration if a later rung needs it). | "Adds within 10" holds about 40 usable (a, b) pairs in total; four bands each wanting 50 distinct questions needs 200, which the numbers cannot supply however the code is written. The 50 was a proxy for "enough variety that no child repeats a question and no two children share one" — at 24-46 a band, with a 21-day exposure window and 16 children, that still holds. | nimish (chat, 2026-09-19, chose "relax the bar" over widening ranges or re-identifying word problems); ADR 0011; `BUILD-ORDER.md` W1 gate 2 |
| 2026-09-19 | **Code enumerates the bank from the skill-set rule; the model writes language once per pattern, never once per question (ADR 0010, supersedes ADR 0005's demotion of the samplers).** `bank.fill` defaults to the enumerator; word problems are sentence templates with number slots the model writes once per skill set; the validator checks templates and a ≤5 % sample of items, not every item; per-item model generation stays only for explain-a-claim / find-the-mistake and as the eval oracle. W1 gate 6: the whole 64-unit bank under ₹50 of model spend. | Nimish, on a ₹1,500 estimate for 3,200 questions: "an infinite loop… generates questions in a loop, is how you would do it. You won't keep spending tokens on every single question, because the questions are in a similar pattern only." Correct: the space is finite and enumerable; the per-item design's cost grows with exactly the thing W1 exists to produce. The enumerator already existed (`bank._sampled`, `assess/items.py`) and had been demoted. | nimish (chat, 2026-09-19); ADR 0010; `BUILD-ORDER.md` W1 |
| 2026-09-19 | **The build order is one workflow at a time, perfected before the next — W1 build-the-bank → W2 assemble-and-print → W3 read-and-graph → W4 close-the-loop — written in `BUILD-ORDER.md` with six machine-checkable gates per workflow, read first by every session.** No code for W(n+1) while a W(n) gate is unchecked. QR identification is a routing lookup inside W3, never mixed into the reading engine, which is developed and measured against the 84 real uploaded sheets. The spine plan's chunk sequencing is superseded; its designs stand and are pulled in by the gates that need them. | Three sessions moved across the map (Growth screen → service foundations → HTTP surface → nearly QR) and left every part incomplete: 4 of 16 rungs specced, 2 of 64 skill×difficulty units filled, 10 of 84 real sheets read, reading accuracy never measured. Nimish: "you could have easily developed module by module… you perfect what you need to build first… you will forget this context because you're not writing everything properly in the handoff." The fix is a sequence in the repo with gates, not another plan in chat. | nimish (chat, 2026-09-19); `BUILD-ORDER.md`; `HANDOFF.md` |
| 2026-09-19 | Chunk 2 of the spine shipped: the engine as a real FastAPI service (`/health`, `/runs/{id}`, `/children/find`, `/ingest`, `/mark`, `/commit`, `/graph/rebuild`, `/bank/fill`), every write route wrapped in `run_idempotent` (a real Postgres partial-unique-index claim, not an in-memory check), containerized and verified from inside a running container against the real database, not mocked. `/ingest` was not split into `/ingest`+`/read`, and `/prescribe`/`/assemble`/`/render` were not built — both deferred with reasons in the plan file, not silently dropped. | Nimish: "make sure you're doing the right rigor of testing." Two real bugs surfaced only because verification insisted on a real Docker build and a real HTTP call rather than trusting unit tests alone — `db.py`'s `REPO_ROOT` computation crashed the container at import time, and an `InProgress` exception was never translated to an HTTP response. Both are now fixed and separately regression-tested. | nimish (chat); `docs/superpowers/plans/2026-09-18-spine.md` chunk 2; STATE.md N3.2 |
| 2026-09-19 | The spine: n8n arrives at F3 (N8–N10) now, not at N7 as `ARCHITECTURE.md` §8 said (ADR 0008); the engine gains an HTTP surface with idempotency keys; a subject is a row plus a prompt scope plus an optional verifier module (ADR 0009); the learning loop is corrections → `read_correction`/`gold` rows → a rebuilt `child_reading_profile` and a re-evaluated prompt, never model training (ADR 0007). Chunk 1 shipped: capture is idempotent on content hash (`file_sha256`/`superseded_by`), the three active read prompts moved from Sonnet to Haiku, a daily spend budget row the adapter refuses past, and PyMuPDF replaces the `pdftoppm` subprocess. | The September batch was imported by hand twice over the same files because nothing could tell it had already run; every paper's evidence counted double. n8n and an idempotent `/ingest` make that structurally impossible instead of a discipline problem. | nimish (chat, "you completely keep ignoring my advice… should be very clear N8N-driven workflows"); `docs/superpowers/plans/2026-09-18-spine.md` |
| 2026-09-17 | No agent harness (ADR 0006): six single-shot prompts behind `adapters/llm.py`; the loop is n8n, the decisions are code or a person. | Nothing in N1–N12 needs a model choosing tools at run time; a harness would put a thinking loop inside the engine, which rule 3 forbids for n8n for the same reason. | nimish ("let's go ahead"), engineer |
| 2026-09-17 | Silence is approval for Thursday practice packs, never for Friday assessments. A threshold row when W2's approval wait is built. | One tap per class per day is the ceiling on teacher attention; practice is low-stakes, assessment feeds the graph. | nimish (chat) |
| 2026-09-17 | Free-tier reality for W1: batches of 20, five waits up to 60 s per model, ordered fallback from a `config` row; `gemini-2.5-flash` is retired for new users and removed. A 200-item fill is a background job, not an interactive one. | Two real fills died on rate limits after 4–8 calls at ~20k tokens each; the third landed 40 verified items before the limit. Patience is free in a background job. | STATE.md, flow_run rows |
| 2026-09-17 | Item generation is prompt-driven (ADR 0005): the skill-set spec — topic, learning objective, skill, difficulty rule in words, philosophy, formats, misconceptions — is rows; one versioned `item_generate` prompt writes the items with answers and distractors; code verifies every number and rejects what fails; any staff member can flag an item, which retires it and feeds the eval set. The code-first samplers become fallback and eval oracle, not the way a topic is added. Flows renamed W1–W4 (bank, print, read-and-graph, close-the-loop). | "No coding to make questions" is the requirement for an engine that must work for any topic in a school without an engineer. Measured first: 20 Hard subtraction items, arithmetic right on all 20, 77/77 distractors matched the predictors, 3 malformed items caught by the verifier. | nimish (chat); `research/2026-09-17-prompt-generation-spike.md` |
| 2026-09-17 | Restore the item bank from spec v0.1, with **the template as the unit of trust**: Aseem ratifies a skill-set spec once and reads a ~20-item sample the first time it generates; code verifies every item; rows land `approved`. No per-item review queue, ever. Human attention goes to a one-tap pack approval per class per day, doubtful reads only, and one Friday batch for notes. An earlier version of this row had a named reviewer approving each item; the workflow (N2) overrides it. | A bank of hundreds of thousands of items cannot have a reader; a teacher cannot approve eleven worksheets a day. Trust is placed where a person can actually exercise it. | workflow v1 N2, N7; nimish (chat) |
| 2026-09-17 | Sheets are assembled per child, seeded from the child — not two class variants. Ten children at the same level get ten different papers of identical difficulty. Picker draws without replacement within a class and excludes anything that child saw in 21 days. | Copying from a neighbour gains nothing and the teacher still holds one answer key. | nimish (chat) |
| 2026-09-17 | Three n8n flows, matching v0.1: F1 build-the-bank (on demand per rung, ends at the reviewer), F2 assemble-and-print (per child, approved items only), F3 read-and-respond (capture → mark → confirm → graph → prescribe → worksheet, assessment and home sheet). Replaces the earlier F1/F2/F3 split, which conflated bank-building with assembly. | Bank-building and weekly assembly have different cadences and different human gates; merging them meant unreviewed items could print. | nimish (chat) |
| 2026-09-17 | Vision reading uses Gemini Flash free tier for the pilot, behind an adapter that masks the child's name before any image is sent (ADR 0004). Revisit at the first non-founder class run or at production. | Free and fast to start; masking makes the free tier's training terms acceptable because what leaves the machine is anonymous. | nimish (chat) |
| 2026-09-17 | Supabase project `cornerstone-cloud` (ref `ruznbyngtfjsaymuylhm`, ap-south-1) is the shared backend for every Cornerstone product, not just this engine. CLI access via a repo-scoped `SUPABASE_ACCESS_TOKEN`, because the machine's default Supabase login is a different personal account. | One database is the point — "how is this child doing" must be one join. | nimish (chat) |
| 2026-09-17 | Per-child adaptive assignment is in scope from Phase 2: the nightly rebuild prescribes each child's next sheet (rule in SPEC §5, thresholds as rows); the class matrix is only the fallback for children without enough evidence. Reverses SPEC §15's earlier exclusion. | Four assessments per child give the graph enough evidence on day one; a class-level sheet would waste it. | nimish (chat) |
| 2026-09-16 | Build the assessment module in full now; the rest of the Learning OS follows step by step. | Four real assessments per child already exist; a real trajectory is the fastest proof. | nimish (chat) |
| 2026-09-16 | Stack: Python engine + Supabase Postgres + self-hosted n8n + Next.js (ADR 0001). | Reuse validated code; one database; requested orchestrator. | nimish delegated: "you are the technical founder" |
| 2026-09-16 | Skill model: registry skill → rung → case tags (ADR 0002). Team taxonomy §12 adopted as the tag matrix. | No fourth taxonomy; coverage guaranteed by tags. | team taxonomy PDF; spec v0.1 §0 |
| 2026-09-16 | n8n orchestrates only; prompts, rules, thresholds are rows (ADR 0003). | Diffable, testable, evaluable. | spec v0.1 §7; nimish |
| 2026-09-16 | Phase 1 = legacy import of the existing 13 scans, before generation. | First real trajectory; first real test of vision reading. | scan check: 0/13 have markers |
| 2026-09-16 | Repo lives at `cornerstonepune/assessment-engine`, local checkout inside `~/cornerstone/`; parent folder is not a repo. | Org ownership; raw data and design docs stay outside git. | nimish (chat) |
