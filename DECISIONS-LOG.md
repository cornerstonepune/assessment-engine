# Decisions log

Newest first. One line per decision: date, decision, why, source. Rejected alternatives get an ADR.

| Date | Decision | Why | Source |
|---|---|---|---|
| 2026-09-17 | Item generation is prompt-driven (ADR 0005): the skill-set spec — topic, learning objective, skill, difficulty rule in words, philosophy, formats, misconceptions — is rows; one versioned `item_generate` prompt writes the items with answers and distractors; code verifies every number and rejects what fails; any staff member can flag an item, which retires it and feeds the eval set. The code-first samplers become fallback and eval oracle, not the way a topic is added. Flows renamed W1–W4 (bank, print, read-and-graph, close-the-loop). | "No coding to make questions" is the requirement for an engine that must work for any topic in a school without an engineer. Measured first: 20 Hard subtraction items, arithmetic right on all 20, 77/77 distractors matched the predictors, 3 malformed items caught by the verifier. | nimish (chat); `research/2026-09-17-prompt-generation-spike.md` |
| 2026-09-17 | Restore the item bank from spec v0.1, with **the template as the unit of trust**: Aseem ratifies a skill-set spec once and reads a ~20-item sample the first time it generates; code verifies every item; rows land `approved`. No per-item review queue, ever. Human attention goes to a one-tap pack approval per class per day, doubtful reads only, and one Friday batch for notes. An earlier version of this row had a named reviewer approving each item; the workflow (N2) overrides it. | A bank of hundreds of thousands of items cannot have a reader; a teacher cannot approve eleven worksheets a day. Trust is placed where a person can actually exercise it. | workflow v1 N2, N7; nimish (chat) |
| 2026-09-17 | Sheets are assembled per child, seeded from the child — not two class variants. Ten children at the same level get ten different papers of identical difficulty. Picker draws without replacement within a class and excludes anything that child saw in 21 days. | Copying from a neighbour gains nothing and the teacher still holds one answer key. | nimish (chat) |
| 2026-09-17 | Three n8n flows, matching v0.1: F1 build-the-bank (on demand per rung, ends at the reviewer), F2 assemble-and-print (per child, approved items only), F3 read-and-respond (capture → mark → confirm → graph → prescribe → worksheet, assessment and home sheet). Replaces the earlier F1/F2/F3 split, which conflated bank-building with assembly. | Bank-building and weekly assembly have different cadences and different human gates; merging them meant unreviewed items could print. | nimish (chat) |
| 2026-09-17 | Vision reading uses Gemini Flash free tier for the pilot, behind an adapter that masks the child's name before any image is sent (ADR 0004). Revisit at the first non-founder class run or at production. | Free and fast to start; masking makes the free tier's training terms acceptable because what leaves the machine is anonymous. | nimish (chat) |
| 2026-09-17 | Supabase project `cornerstone-cloud` (ref `ruznbyngtfjsaymuylhm`, ap-south-1) is the shared backend for every Cornerstone product, not just this engine. CLI access via a repo-scoped `SUPABASE_ACCESS_TOKEN`, because the machine's default Supabase login is a different personal account. | One database is the point — "how is this child doing" must be one join. | nimish (chat) |
| 2026-09-17 | Per-child adaptive assignment is in scope from Phase 2: the nightly rebuild prescribes each child's next sheet (rule in SPEC §5, thresholds as rows); the class matrix is only the fallback for children without enough evidence. Reverses SPEC §15's earlier exclusion. | Four assessments per child give the graph enough evidence on day one; a class-level sheet would waste it. | nimish (chat) |
| 2026-09-16 | Build the assessment module in full now; the rest of the Learning OS follows step by step. | Four real assessments per child already exist; a real trajectory is the fastest proof. | nimish (chat) |
| 2026-09-16 | Stack: Python engine + Supabase Postgres + self-hosted n8n + Next.js (ADR 0001). | Reuse validated code; one database; requested orchestrator. | nimish delegated: "you are the technical founder" |
| 2026-09-16 | Skill model: registry skill → rung → case tags (ADR 0002). Team taxonomy §12 adopted as the tag matrix. | No fourth taxonomy; coverage guaranteed by tags. | team taxonomy PDF; spec v0.1 §0 |
| 2026-09-16 | n8n orchestrates only; prompts, rules, thresholds are rows (ADR 0003). | Diffable, testable, evaluable. | spec v0.1 §7; nimish |
| 2026-09-16 | Phase 1 = legacy import of the existing 13 scans, before generation. | First real trajectory; first real test of vision reading. | scan check: 0/13 have markers |
| 2026-09-16 | Repo lives at `cornerstonepune/assessment-engine`, local checkout inside `~/cornerstone/`; parent folder is not a repo. | Org ownership; raw data and design docs stay outside git. | nimish (chat) |
