--- a/lib/queries.ts
+++ b/lib/queries.ts
@@ -1,5 +1,6 @@
 import { sql } from "@/lib/db";
 
+// ---- Plain English name mappings
 
 export const RULE_WORDS: Record<string, string> = {
   band_default: "Not enough of their own work yet, so this is the starting level for their grade.",
@@ -6,6 +7,43 @@ export const RULE_WORDS: Record<string, string> = {
   from_state: "From what this child's last papers showed.",
   override: "A teacher set this by hand.",
 };
+
+const RUNG_NAMES: Record<string, string> = {
+  R4: "Adding 2-digit numbers without regrouping",
+  R5: "Adding 2-digit numbers with regrouping",
+  R6: "Subtracting 2-digit numbers with exchange",
+  R7: "Using mental strategies: bridging, friendly numbers, missing numbers",
+  R8: "One- and two-step word problems with money or objects",
+  R9: "Adding or subtracting 3-digit numbers",
+  R10: "Subtracting 3-digit numbers across zeros",
+  R11: "Estimating and judging if answers make sense",
+  R12: "Large numbers with multiple regroupings",
+  X1: "Explaining how they solved it or checking someone else's work",
+};
+
+const STATE_NAMES: Record<string, { label: string; description: string }> = {
+  not_enough_yet: { label: "Not ready", description: "Needs more practice" },
+  emerging: { label: "Starting", description: "Learning this skill" },
+  practising: { label: "Practising", description: "Getting better at this" },
+  patterned_error: { label: "Repeating a mistake", description: "Makes the same error over and over" },
+  secure: { label: "Got it", description: "Can do this reliably" },
+  stretch_ready: { label: "Ready to move up", description: "Ready for the next challenge" },
+};
+
+const MISCONCEPTION_NAMES: Record<string, string> = {
+  M_NOCARRY: "Forgets to carry when adding",
+  M_FACT_PM1: "Off by 1 in basic facts",
+  M_FACT_PM10: "Off by 10 in basic facts",
+  M_EXCHANGE_NEED: "Doesn't regroup when needed",
+  M_EXCHANGE_UNNEED: "Regroups when not needed",
+  M_ACROSS_ZERO: "Struggles with zeros in subtraction",
+  M_DIRECTION: "Does subtraction backwards",
+  M_BORROWS_WRONG: "Borrows from wrong place",
+  M_WRITES_REMAINDER: "Writes remainders as part of answer",
+};
 
 export type ChildRow = {
   id: string;
