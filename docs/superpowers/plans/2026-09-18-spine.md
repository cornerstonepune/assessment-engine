# The spine: engine as a service, n8n workflows, validators, subject plugins, learning loop

Repo copy after approval: `docs/superpowers/plans/2026-09-18-spine.md`. ADRs 0007–0009 and
DECISIONS-LOG rows are part of chunk 1.

## Context

`ARCHITECTURE.md` §1–§6 and ADR 0003 already say: the engine is an HTTP service, n8n holds the
triggers/sequences/waits, prompts and rules are rows, humans confirm. None of the HTTP surface or
n8n exists (`packages/engine` has no `api/`; there is no `n8n/workflows/`; n8n is not installed).
Every step has been run by hand from the CLI — which is how each paper got imported twice
(`legacy.import_scan` inserts a fresh `capture` unconditionally; 6 child×paper pairs have two
reads each, 43 "confirmed" answers on a 24-question paper). The read prompts are configured on
`claude-sonnet-5` in the `prompt` table — that is where the $10 went. `gold` exists and is empty;
`read_confidence` and `read.auto_confirm_above` are never written or read; there is no validator
step on reads or on generated items; nothing feeds a teacher's correction back into reading.
`verify.py`, `mark()`, `rung_for()` and the `item_generate` prompt assume `a op b` arithmetic, so
a second subject cannot be added by rows today, though the data model (`skill.domain`, `tenant_id`
on every table, `item.spec` jsonb, `prompt` rows) already can.

Nimish's requirements, verbatim in intent: build it the right way now; it scales across subjects
and concurrent users within ~12 days; item creation is one master prompt per subject plus the
skill map; there are validation/confirmation layers on questions and on reads; the system gets
better at reading and understanding as it ingests more and as teachers correct it.

**Outcome:** dropping a scan in a folder produces candidates, a validator's verdict and a queue
count without anyone typing a command, and doing it twice changes nothing; a new subject is rows
plus at most one verifier module; every teacher correction becomes memory, gold and routing.

## Decisions taken (change any on review)

| # | Decision | Why |
|---|---|---|
| D1 | **n8n now, self-hosted in Docker, F3 first.** Supersedes `ARCHITECTURE.md` §8 "n8n arrives at N7". | The hand-run CLI is what produced the double import; the first two days *are* the read loop. ADR 0003 already rejected n8n Cloud (children's photos through a third party). |
| D2 | **n8n carries ids only** — capture ids, run ids, counts. Never a name, a path with a name, or an image. | n8n stores every node's payload in its execution log; rule 6 says scans never enter a log. A CI lint enforces it. |
| D3 | **Duplicates are voided, not deleted:** `capture.superseded_by`; the graph reads only live captures. | Rule 4, evidence is append-only. The rows stay; they stop counting. |
| D4 | **Validators are single-shot prompts** (`validate_read`, `validate_item`), each a `prompt` row with a JSON schema, gold and an eval. | Consistent with ADR 0006 (no harness). "A small agent that validates" = one fixed-shape call whose verdict code then acts on. |
| D5 | **Subject = a `subject` row + prompts keyed by subject + an optional verifier module.** Verifier is decisive on numbers where it exists (maths); the validator prompt + a ratified first sample is the guarantee where it does not. | Extends ADR 0005. Code can't verify a biology answer; a model plus a person can. |
| D6 | **The learning loop is rows + retrieval + eval gating, not model training.** Corrections → per-child reading notes and few-shot examples in the prompt, gold rows, and routing that adapts. | Frontier vision models cannot be fine-tuned by us; the memory and the prompt can learn. New ADR 0007. |
| D7 | **Haiku for every read and validate; Sonnet only by explicit row change.** A daily budget row; the adapter refuses past it. | Nimish: "use haiku or sonnet for majority/all tasks… use the key judiciously". |
| D8 | **Pilot on this Mac (Docker compose: engine + n8n); VPS in Mumbai before any teacher-facing run.** | SPEC §14.5. Recommend a ₹1,500–2,500/mo VPS; Nimish's spend decision, not blocking the pilot. |
| D9 | **Pilot notification = the Capture screen's queue count; no WhatsApp/email node yet.** | Channel is an open decision (SPEC §14.2). Adding a node later is one box. |
| D10 | **Drive trigger later; inbox folder now.** n8n's Local File Trigger on `~/cornerstone/assessments/inbox/`. | The Google OAuth click is Nimish's; the folder needs nothing and proves the flow. |
| D11 | **FastAPI handlers are plain `def` over the existing sync psycopg `db.connect()`**, with a `psycopg_pool`. | Rule 10 (extend, don't rewrite); FastAPI runs sync handlers in a threadpool. Deliberate exception to the async-handler house rule, recorded in ADR 0008. |

## Architecture after this plan

```
inbox/ or Drive ──n8n F3──> POST /ingest (idempotent: sha256+child+paper)
                            POST /read   (read prompt → validate_read prompt → confidence)
                            POST /mark   (lookup or rubric, by subject)
                            n8n writes nothing but ids; Capture screen shows "N to confirm"
Achal confirms/settles in the app ──> confirm_results / resolve_result (SQL, unchanged)
                                      + read_correction + gold rows (new)
n8n F3-nightly ──> POST /graph/rebuild → POST /profiles/rebuild → POST /bank/fill (top-up)
n8n F1 ──> POST /bank/fill {subject, skill_set, difficulty} (generate → verifier? → validate_item → sample|approved)
```

Engine layout additions (all inside `packages/engine/engine/`):
`api/app.py` `api/deps.py` `api/routes/{ingest,read,mark,commit,graph,bank,week,runs}.py`
`subjects/__init__.py` `subjects/maths.py` (moved: `assess/verify.py` rules, `assess/misconceptions.py` predictors, `legacy.rung_for/parse_expr/mark`)
`learning.py` (corrections → profile, gold, routing) · `render_pdf.py` (PyMuPDF, replaces the `pdftoppm` subprocess)

## Chunks

Each chunk is one commit, tests first for anything with a branch or a parser, `STATE.md` and
`HANDOFF.md` updated at the boundary. Success criteria are commands.

### Chunk 1 — foundations for a service (day 1)

Files: `supabase/migrations/20260919090000_service_foundations.sql`, `engine/db.py`,
`engine/adapters/llm.py`, `engine/legacy.py`, `engine/render_pdf.py`, `supabase/seed/prompts.json`,
`supabase/seed/config.json`, `docs/adr/0007-learning-loop.md`, `0008-engine-service.md`,
`0009-subject-plugins.md`, `DECISIONS-LOG.md`.

- Migration: `capture.file_sha256 text`, `capture.superseded_by uuid`, unique
  `(tenant_id, sheet_instance_id, file_sha256) where superseded_by is null`; `flow_run.idempotency_key
  text` + `flow_run.request jsonb` (ids only) + unique `(tenant_id, flow, idempotency_key)`;
  `prompt.subject text null` + unique `(tenant_id, purpose, coalesce(subject,''), version)`;
  `subject(code, name, verifier text null, mark_mode text)` with row `NUM`; `read_correction(child_id,
  capture_id, item_result_id, model_read, human_read, misconception_codes, by, created_at)`;
  `child_reading_profile(child_id, notes jsonb, updated_at)` (ring B, truncatable); threshold rows
  `read.route_above_overturn 0.25`, `llm.daily_budget_inr`; `rebuild_child_skill_state`,
  `next_difficulty`, `confirm_results` and `childMap`/`childEvidence`/`pendingResults` in
  `apps/web/lib/queries.ts` join `capture` and exclude `superseded_by is not null`.
- `llm.generate(conn, purpose, variables, images=(), subject=None)`: prefer the row with matching
  `subject`, else `subject is null`; compute `cost_inr` from a `config llm.prices` row; refuse with
  `LLMError` when today's `sum(cost_inr)` for the tenant exceeds the budget row.
- Prompt rows: `legacy_extract`, `read_cells`, `read_page` → `model = claude-haiku-4-5` (seed +
  `engine load`). Sonnet only by a new version row.
- `render_pdf.py`: PyMuPDF (`pymupdf` dep) replaces `subprocess pdftoppm`; same `render_pages()`
  signature; the 13 errored captures re-run through chunk 2's `/ingest`.
- `engine legacy dedupe`: for each (sheet_instance, file_sha256) pair with more than one live
  capture, set `superseded_by` on all but the latest; print what it voided. Also
  `import_scan` computes the sha256 and returns the existing capture when one is live.

Criteria:
1. `cd packages/engine && uv run pytest -q` green, including new tests: re-import of the same file
   returns the same `capture_id`; a Sonnet row is not chosen when a Haiku row is active; the
   adapter refuses above budget; `render_pages` renders one of the previously failing PDFs.
2. `uv run engine load --check` → zero diff, `prompt` rows show `claude-haiku-4-5` for the three
   read purposes.
3. `uv run engine legacy dedupe && uv run engine graph` → voided count printed; then
   `psql -Atc "select count(*) from evidence_event e join item_result r on r.id=e.item_result_id join capture c on c.id=r.capture_id where c.superseded_by is null and e.confirmed_by is not null"`
   ≈ 109 (was 218), and no child×paper pair has two live captures.
4. Child page for Advika shows each question once under a step.

### Chunk 2 — the engine as a service (day 2)

Files: `engine/api/app.py`, `engine/api/deps.py`, `engine/api/routes/*.py`, `engine/api/models.py`
(Pydantic v2), `packages/engine/Dockerfile`, `deploy/compose.yml`, `packages/engine/tests/api/`.
Deps: `fastapi`, `uvicorn[standard]`, `psycopg_pool`, `httpx` (tests).

- `X-Engine-Key` header checked in a dependency; key from env `ENGINE_KEY`; 401 otherwise.
- Every route: `Idempotency-Key` header (or derived: sha256 of the body's ids) → `flow_run`
  row; a repeat returns the stored result with `already: true`. Request/response bodies carry
  ids and counts only.
- Routes over existing functions: `/ingest` → `legacy.import_scan` split into `ingest()` (capture
  only) and `read()`; `/read` → the read prompt + (chunk 3) validator; `/mark` → `legacy.mark`
  per result; `/commit` → `confirm_results`; `/graph/rebuild` → `rebuild_child_skill_state`;
  `/bank/fill` → `bank.fill`; `/prescribe` `/assemble` `/render` → `prescribe.py`, `assemble.py`;
  `/runs/{id}` → `flow_run`; `/health` → `select 1`.
- `db.py`: a module-level `ConnectionPool(min_size=1, max_size=8)` when `ENGINE_POOL=1`; CLI keeps
  `connect()`. Transaction pooler (6543) with `prepare_threshold=None`.
- Dockerfile (uv, python 3.12), `deploy/compose.yml` with `engine` and `n8n` services, an
  `.env.example` for both.

Criteria:
1. `uv run pytest -q tests/api` green: 401 without key; `POST /ingest` twice with the same file →
   same `capture_id`, second has `already: true`; `/runs/{id}` shows tokens and cost.
2. `docker compose -f deploy/compose.yml up -d engine && curl -s localhost:8000/health` → `{"ok":true}`.
3. `curl -X POST localhost:8000/graph/rebuild -H "X-Engine-Key: …"` → `{"children": 4}` and a
   `flow_run` row with `trigger = 'http'`.

### Chunk 3 — validate the read, capture corrections, grow gold (days 3–4)

Files: `supabase/seed/prompts/validate_read.v1.txt` (+ schema in `prompts.json`),
`engine/learning.py`, `engine/api/routes/read.py`, `supabase/migrations/…_resolve_with_read.sql`
(`resolve_result` gains `p_read text`, writes `read_correction` + `gold`), `apps/web/app/(app)/growth/[id]/page.tsx`
+ `actions.ts` (the settle form gets a "child wrote" field and a misconception picker),
`engine/cli.py eval`.

- `validate_read` v1: input = the masked page + the transcription; output per answer
  `{n, part, agree, suggested, reason}` and `page_ok`. Haiku.
- Confidence and routing (code): `read_confidence = 1.0` when validator agrees, `0.5` when it
  suggests a different value, `0.0` when unreadable; route to the queue when confidence <
  `read.auto_confirm_above` **or** the (child, format) overturn rate over 30 days >
  `read.route_above_overturn`. Everything else stays a candidate for one-tap confirm.
- Every settle with an edited read → `read_correction` + `gold(truth)`; every plain confirm of a
  validator-agreed read → `gold` too (agreement is also truth).
- `engine eval legacy_extract|read_cells [--version N]`: run the prompt over every gold capture,
  print agreement per field; `--activate` flips `active` only if agreement ≥ the active version's.

Criteria:
1. Tests: validator disagreement routes to the queue; agreement does not; a settle with an edit
   writes `read_correction` and `gold`; eval refuses to activate a worse version.
2. `uv run engine eval legacy_extract` prints `agreement N/M` over ≥ 20 gold rows after Achal's
   first sitting.
3. On the child page, settling an answer with a typed correction shows the corrected value in the
   step's table on reload.

### Chunk 4 — n8n F3: read-and-respond (days 4–5)

Files: `deploy/compose.yml` (n8n service, `N8N_ENCRYPTION_KEY`, executions pruned at 7 days),
`n8n/workflows/f3-read-and-respond.json`, `n8n/workflows/f3-nightly.json`, `n8n/lint.py`,
`n8n/README.md` (import/export commands), `.github/workflows/ci.yml` (lint job).

- F3: Local File Trigger on `~/cornerstone/assessments/inbox/{section}/{child}/…pdf` → HTTP
  `POST /ingest {path_id}` (the engine maps a path to child + paper via `roster.find` and the
  paper code in the folder name; n8n never sees a name) → `POST /read` → `POST /mark` → IF
  `queue > 0` → write a `flow_run` note; the Capture screen reads it. Errors → retry ×3 with
  backoff → a `flow_run` error row.
- F3-nightly (02:00 IST): `POST /graph/rebuild` → `POST /profiles/rebuild` (chunk 6) →
  `POST /bank/fill` for any skill_set×difficulty under `bank.min_pool` (threshold row).
- `n8n/lint.py`: fails CI if any workflow has a Code node, a Set node with a string over 200
  chars, an `n8n-nodes-base.openAi`/LLM node, or a parameter mentioning `name`, `first_name`,
  `image`, `base64`.
- Node names verified against the installed n8n version (ADR 0003 consequence) — record the
  version in `n8n/README.md`.

Criteria:
1. `docker compose up -d` → `http://localhost:5678` up; `n8n import:workflow --input n8n/workflows/f3-read-and-respond.json` succeeds.
2. Copy one PDF into the inbox → within 60 s: one `capture`, its candidates, a `flow_run` with
   `trigger = 'n8n'`; `/capture` shows "N to confirm". Copy the same PDF again → no new capture,
   `already: true` in the run.
3. `python n8n/lint.py` passes; a workflow with a Code node fails it (test fixture).
4. Re-drop the 13 previously failed PDFs → all render and read (PyMuPDF), zero `error` captures.

### Chunk 5 — subject plugins, validate_item, sample ratification (days 6–8)

Files: `engine/subjects/__init__.py`, `engine/subjects/maths.py` (moved from `assess/verify.py`
rule checks, `assess/misconceptions.py` predictors, `legacy.rung_for/parse_expr/mark`),
`engine/bank.py`, `supabase/seed/prompts/{item_generate.NUM.v2,item_generate.v2,validate_item.v1}.txt`,
`supabase/migrations/…_subjects.sql` (`skill_set.status` gains `live`; `item.status` gains
`sample`), `apps/web/app/(app)/skill-sets/[code]/page.tsx` (ratify the sample → live),
`engine/assess/render.py` (`mcq`, `short_text` formats), `engine/api/routes/bank.py`.

- `subjects.verifier(code)` returns the module named in the `subject` row or `None`.
  `bank.fill`: generate with the subject's `item_generate` row (subject-specific master prompt,
  else generic) → verifier `problems()` if present → `validate_item` verdict → status `sample`
  until the skill set is `live`, then `approved`. Failures counted in `flow_run` by reason.
- `validate_item` v1: input = item + spec + philosophy; output `{ok, problems[]}` on: answerable,
  one correct answer, matches objective and the difficulty words, language for the grade, no
  forbidden words, distractors plausible. Haiku.
- The maths master prompt keeps `op/a/b/answer`; the generic one asks for `{question, answer,
  rationale, options?, rubric?}`; `item.spec` stores either; `mark_mode` from the `subject` row:
  `lookup` (numbers/options) or `rubric` (→ `needs_teacher`, rubric shown in the queue).
- Legacy import rung: from the paper definition (already confirmed by the person entering it);
  the maths plugin only *suggests*.

Criteria:
1. Existing tests green with `verify`/`misconceptions` imported through `subjects.maths`.
2. Proof by rows: insert a `subject` row `SCI`, three skills, two rungs, one skill set, an
   `item_generate` row with `subject = 'SCI'`, then
   `uv run engine bank fill --set SCI.LIFE.1 --difficulty Medium --n 5` → 5 items with
   `status = 'sample'`, each carrying a `validate_item` verdict; `git diff --stat` shows Python
   changes only under `engine/subjects/` and `render.py` (recorded in STATE.md).
3. Ratifying the sample on the skill-set screen flips `status` to `live`; the next fill lands `approved`.
4. `uv run engine eval item_generate --subject NUM` pass rate ≥ today's.

### Chunk 6 — the loop closes: reading profiles in the prompt (days 9–10)

Files: `engine/learning.py` (`rebuild_profiles`), `engine/api/routes/profiles.py`,
`supabase/seed/prompts/{legacy_extract.v3,read_cells.v2}.txt` with `{{child_notes}}` and
few-shot corrected crops passed as images, `engine/adapters/llm.py` (`examples=` images).

- `rebuild_profiles`: per child, from `read_correction`: digit-confusion tallies (`7→1`, `4→9`),
  habits (`answers in margin`, `crosses out`), the last 5 corrected crops (paths under `data/`,
  never in the DB as bytes). Code only, no model.
- The read call for a child injects notes + up to 5 example crops with their true values.
- Activation gated by `engine eval … --activate` (chunk 3): v3 must match or beat v2 on gold.

Criteria:
1. `psql -Atc "select count(*) from child_reading_profile where notes <> '{}'"` ≥ number of
   children with corrections.
2. `uv run engine eval legacy_extract --version 3` ≥ version 2's agreement; then activated.
3. A read of a child with a `7→1` note transcribes a 7 the previous version misread (one gold case
   named in STATE.md).

### Chunk 7 — CI/CD and deploy (day 11)

Files: `.github/workflows/ci.yml` (add `api` tests, docker build, `n8n/lint.py`), `deploy/README.md`
(runbook: compose up, import workflows, rotate `ENGINE_KEY`, backups of n8n's sqlite),
`apps/web/lib/engine.ts` (the app's server actions call the engine for `/bank/fill` and
`/prescribe`; confirm/resolve stay SQL).

Criteria:
1. A PR runs engine tests, API tests, web `tsc` + build, and the n8n lint; all green on main.
2. `docker compose -f deploy/compose.yml up -d` on a clean checkout with `.env` → `/health` and
   n8n both answer.
3. Vercel: push to main deploys `apps/web` (Nimish reconnects the GitHub integration once;
   `AUTH_SECRET` and `DATABASE_URL` present).

## What Nimish provides, and when

| When | What | Why it has to be you |
|---|---|---|
| Chunk 4, first launch | Open `http://localhost:5678` once and create the n8n **owner account** (email + password). ~1 minute. | Account creation is yours; I run the container, generate `N8N_ENCRYPTION_KEY`, and import workflows from the CLI, which needs no login. |
| Chunk 4 | In n8n → Settings → n8n API → create a key; paste it into `.env` as `N8N_API_KEY`. | Lets the CI/engine read execution logs and re-run a failed job. Same handling as the other keys in `.env`. |
| Chunk 4 (optional now) | Nothing else. The inbox folder trigger needs no credentials. | — |
| Drive trigger (later, one node swap) | A Google Cloud OAuth client (Drive API enabled) connected in n8n → Credentials → Google Drive. | OAuth consent is a click only you can make. |
| N7 / notifications (later) | WhatsApp: a Meta Business account and Cloud API token; or Gmail OAuth for email. | Business-account ownership. |
| D8 (before a teacher-facing run) | Pick and pay for a VPS in Mumbai (~₹1,500–2,500/mo). I set it up from a root SSH key you generate. | Spend. |

n8n Cloud needs none of this but was rejected in ADR 0003 (children's work would transit a third party); nothing changes there.

## Verification, end to end (after chunk 4, repeated after 7)

1. Drop Heian's Cambridge PDF in `inbox/G2/Heian/` → F3 runs; `/capture` shows the count;
   settle two answers with a typed correction; confirm the rest → Child Growth ladder rebuilt,
   `read_correction` and `gold` rows exist. Drop it again → nothing changes.
2. `uv run engine eval legacy_extract` prints agreement over gold.
3. `uv run engine bank fill --set ADD.2D.REG --difficulty Hard --n 10` through `POST /bank/fill`
   → verifier + validator verdicts in `flow_run`; items `approved`.
4. Screens: `cd apps/web && npm run test:e2e` green; engine `uv run pytest -q` green; CI green.
5. Cost: `psql -Atc "select date(started_at), round(sum(cost_inr),2) from flow_run group by 1 order by 1 desc limit 3"` — every read on Haiku.

## Out of scope here (next plans)

QR/fiducial ingest for generated sheets (N8 proper) · WhatsApp send-and-wait (N7) and the parent
note channel · Drive trigger (one node swap once OAuth is done) · GLM/Kimi generation experiment
(`research/2026-09-18-cheaper-models-glm-kimi.md`) · misconception-example learning for rubric
subjects (loop step 5) · VPS move (D8).
